# Specs

Per-system design notes live here, one file per system, added when the system is designed
(e.g. `zombie-ai.md`, `round-loop.md`, `matchmaking.md`). Each spec covers: purpose, public API,
dependencies, data flow, remotes, and edge cases. See ../ARCHITECTURE.md for the system map.
